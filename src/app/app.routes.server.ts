import { Routes } from '@angular/router';

// Define your server-side routes here
export const serverRoutes: Routes = [
  // You can add specific server-side routes here if needed
  // For most applications, an empty array is sufficient
];
